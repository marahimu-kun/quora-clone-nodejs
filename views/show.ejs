<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Question - Quora</title>
    <link rel="stylesheet" href="/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
</head>

<body>
    <div class="header">
        <div class="container">
            <h1>Quora</h1>
        </div>
    </div>

    <div class="container">
        <div class="post">
            <div class="username">@<%= post.username %>
            </div>
            <div style="margin-bottom: 20px;">
                <div style="color: var(--quora-dark-gray); margin-bottom: 5px;">Posted by @<%= post.username %>
                </div>
                <div style="font-size: 12px; color: var(--quora-dark-gray);">Question ID: <%= post.id %>
                </div>
            </div>
            <div class="content">
                <%= post.content %>
            </div>
            <div class="post-actions">
                <a href="/posts/<%= post.id %>/edit">
                    <i class="fas fa-edit"></i> Edit Question
                </a>
                <form action="/posts/<%= post.id %>?_method=DELETE" method="post" style="display: inline;">
                    <button type="submit">
                        <i class="fas fa-trash"></i> Delete
                    </button>
                </form>
                <a href="/posts">
                    <i class="fas fa-arrow-left"></i> Back to Questions
                </a>
            </div>
        </div>
    </div>
</body>

</html>